var c = 1
 while (c <= 10) {
    let soma = c + 100
    console.log (`O peso da sua mae é ${soma}`) 
        c++} 

              
const nome = 'kaio'
let i = 0;
while(i < nome.length){
    console.log(nome[i])
    i++
}

function random(min, max) {
    const r =  Math.random() * (max - min) + min;
    return Math.floor(r);
}
  
  const min = 1
  const max = 60
  let rand = random(min, max)
  while(rand !== 10) {
   rand = random(min, max)
    console.log(rand)
  }
    
